import { Observable } from 'rxjs';
interface Message {
    from: string;
    message: string;
}
export declare const sendMessage: (message: Message) => void;
export declare const getMessages: () => Observable<Message>;
export {};
